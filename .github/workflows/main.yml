name: Gulf Jobs AI Agent

on:
  workflow_dispatch:
  push:
    paths:
      - "jobs.json"

jobs:
  analyze-jobs:
    runs-on: ubuntu-latest

    steps:
      - name: Download project
        uses: actions/checkout@v4

      - name: Analyze jobs
        run: |
          echo "================================"
          echo "   GULF JOBS AI AGENT"
          echo "================================"
          echo ""
          echo "Available jobs:"
          cat jobs.json
          echo ""
          echo "================================"
          echo "Agent is working successfully!"
          echo "================================"
